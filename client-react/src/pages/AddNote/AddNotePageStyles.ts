import { makeStyles, Theme } from "@material-ui/core/styles";

const useAddNotePageStyles = makeStyles((theme: Theme) => ({
    root: {
        
    },
    form: {
        width: "30%",
        margin: "auto auto",
    }
}));

export default useAddNotePageStyles;