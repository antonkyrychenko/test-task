import { makeStyles, Theme } from "@material-ui/core/styles";

const useEditNotePageStyles = makeStyles((theme: Theme) => ({
    root: {

    },
    form: {
        width: "30%",
        margin: "auto auto",
    }
}));

export default useEditNotePageStyles;